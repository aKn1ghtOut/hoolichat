name: Flutter CI

# This workflow is triggered on pushes to the repository.

on:
  push:
    branches:
    - cicd
    
# on: push    # Default will running for every branch.
    
jobs:
  build:
    # This job will run on ubuntu virtual machine
    runs-on: ubuntu-latest
    steps:
    
    # Setup Java environment in order to build the Android app.
    - uses: actions/checkout@v1
    - uses: actions/setup-java@v1
      with:
        java-version: '12.x'
    
    # Setup the flutter environment.
    - uses: subosito/flutter-action@v1
      with:
        channel: 'stable' # 'dev', 'alpha', default to: 'stable'
        # flutter-version: '1.12.x' # you can also specify exact version of flutter
    
    # Get flutter dependencies.
    - run: flutter pub get
    
    # Statically analyze the Dart code for any errors.
    - run: flutter analyze .
    
    # Run widget tests for our flutter project.
    - run: flutter test
    
    # Build apk.
    - run: flutter build apk

    # Build web files
    - run: flutter build web
    
    # Upload generated apk to the artifacts.
    - uses: actions/upload-artifact@v1
      with:
        name: release-apk
        path: build/app/outputs/apk/release/app-release.apk
        env-url: FOOBAR
        
    # Use URL to upload to FURLS
    - run: curl 'https://furls.herokuapp.com/api/urls/60ad4e509e57750a891d2ef1' -X 'PATCH' -H authorization:\ Token eyJ0eXAiOiJKV1QiLCJhbGciOiJIUzI1NiJ9.eyJfaWQiOiJhOWFkMzBmYmJkMWQ0YThmOWFhNjVlZDEyZDhhNjE1ZiIsImVtYWlsIjoiaXNoYmVzd2FsQGdtYWlsLmNvbSIsIm5hbWUiOiJJc2hpdCBCZXN3YWwifQ.cYRU8-2l7RodRa1wmUnKdACCGpBLIPshiW8e8uHp6yc -H 'content-type:\ application/json' --data-raw '{"original":"${{ env.FOOBAR }}"}' --compressed
